/* import 'package:flutter/material.dart';

class RedemptionTile extends StatelessWidget {
  const RedemptionTile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListTile(
      title: GestureDetector(child:Text()),

    );
  }
} */
